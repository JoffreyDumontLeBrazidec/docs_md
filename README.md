# /home/ecm5702/hpcperm/docs vault map

This tree is the Obsidian browsing and publish surface. It is not the authoritative source for task state, manifests, or canonical experiment authoring.

## Layout
- `published/` for published hub surfaces such as scoreboard landing pages
- `exp/` for published experiment rooms
- `notes/` for personal or Obsidian-only notes

## Working rule
1. Edit the canonical repo-owned source first under `/etc/ecmwf/nfs/dh2_home_a/ecm5702/dev/docs/`.
2. Rebuild any generated repo surface if needed.
3. Publish or refresh the vault copy after the repo version is correct.

## Related paths
- `/home/ecm5702/hpcperm/reference_grib/` holds vault-side companion reference GRIB files used by some published rooms
- `/etc/ecmwf/nfs/dh2_home_a/ecm5702/dev/docs/docs/instructions/vault-publishing.md` is the canonical publish policy
- `/etc/ecmwf/nfs/dh2_home_a/ecm5702/dev/docs/docs/instructions/storage-layout.md` is the canonical home-directory storage map
